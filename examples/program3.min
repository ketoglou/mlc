//This is for testing nesting in blocks for idermetiad language

program testBlocks
{
	declare a,b,c,d;
      
	procedure block1(inout a,inout b){
		function block2(in z){
			declare e,f,g;
			procedure block3(){
				function block4(in k){
				
				}
			}
		}
	}
	{
		call block1(in block2(in c-2),inout d);
		d := 3+a+b-23*8+b/2*block2(in z-1)+3533;
		print(d);
		input(a);
		return block2(in a);
	}
}
